import 'package:flutter/material.dart';

class PasswordsBody extends StatelessWidget {
  const PasswordsBody({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Passwords'),
    );
  }
}
