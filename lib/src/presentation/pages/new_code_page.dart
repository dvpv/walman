import 'package:flutter/material.dart';

class NewCodePage extends StatelessWidget {
  const NewCodePage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('New code'),
    );
  }
}
